<%= form_for(current_user.relationcategorias.build(followed_id: @category.id), remote: true) do |f| %>
    <div><%= f.hidden_field :followed_id %></div>
    <%= f.submit "Seguir", class: "btn btn-large btn-primary" %>
<% end %>